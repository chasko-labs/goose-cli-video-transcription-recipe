services:
  whisper:
    build:
      context: ./docker
      dockerfile: whisper-rocm.Dockerfile
    container_name: transcribe-whisper
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
    volumes:
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/videos:/media/videos
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/audio:/media/audio
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/frames:/media/frames
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/transcripts:/media/transcripts
    environment:
      - HSA_OVERRIDE_GFX_VERSION=10.3.0
      - PYTORCH_ALLOC_CONF=expandable_segments:True
      - SCENE_THRESHOLD=0.4
    networks:
      - heraldstack

  vision:
    build:
      context: ./docker
      dockerfile: vision-rocm.Dockerfile
    container_name: transcribe-vision
    devices:
      - /dev/kfd:/dev/kfd
      - /dev/dri:/dev/dri
    group_add:
      - video
      - render
    volumes:
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/frames:/media/frames
      - ${VIDEO_TRANSCRIPTS_DIR:-${HOME}/video-transcripts}/transcripts:/media/transcripts
      - hf-cache:/cache/huggingface
    environment:
      - HSA_OVERRIDE_GFX_VERSION=10.3.0
      - PYTORCH_ALLOC_CONF=expandable_segments:True
      - VISION_MODEL=amd/Instella-VL-1B
    networks:
      - heraldstack

networks:
  heraldstack:
    external: true

volumes:
  hf-cache:
